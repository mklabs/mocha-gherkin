#!/usr/bin/env node

var fs = require('fs'),
  path = require('path');

var Parser = require('../');

var step = process.argv.slice(2)[0];

var parser = new Parser({
  steps: step ? fs.readFileSync(path.resolve(), 'utf8') : null
});

process.openStdin()
  .pipe(parser)
  .pipe(process.stdout);

